apiVersion: v1
kind: Service
metadata:
  name: transaction-service
  namespace: finance-app
spec:
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: NodePort
  selector:
    app: transaction-app
    namespace: finance-app